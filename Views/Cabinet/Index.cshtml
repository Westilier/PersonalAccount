@model StudentModel

@{
    ViewData["Title"] = "Личный кабинет";
}

<div class="container">
    <div class="d-flex justify-content-between align-content-center">
        <h1 class="mb-4">Личный кабинет студента</h1>
        
        <div class="d-flex align-content-end align-content-center gap-5">
            <form method="get" asp-controller="Cabinet" asp-action="Edit">
                <button class="btn btn-secondary" type="submit">Редактировать</button>
            </form>
            <form method="post" asp-controller="Account" asp-action="Logout">
                <button class="btn btn-secondary" type="submit">Выйти</button>
            </form>
        </div>
    </div>

    <div class="card">
        @if (Model.PhotoUrl != null)
        {
            <img class="card-img-top" src="@Model.PhotoUrl" alt="Фото студента"/>
        }
        <header class="card-header">
            @Model.FullName
        </header>

        <div class="card-body">
            <p class="mb-1">
                Группа: @Model.GroupName
            </p>
            <p class="mb-1">
                Email: @Model.Email
            </p>
        </div>
    </div>
</div>